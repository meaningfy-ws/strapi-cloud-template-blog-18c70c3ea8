'use strict';

/**
 * waitlist-submission router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::waitlist-submission.waitlist-submission');
